const circle = require("./circle.js");

//console.log(`The circumference and the area of a circle of radius ${radius} are ${circle.circumference(radius)} and ${circle.area(radius)}`);
console.log(`The circumference and the area of a circle of radius 3 are ${circle.circumference(3)} and ${circle.area(3)}`);